[build]
  publish = "frontend"
  command = "echo 'Frontend is static - no build needed'"

[[redirects]]
  from = "/api/*"
  to = "http://localhost:8000/api/:splat"
  status = 200
  force = true

[build.environment]
  NODE_VERSION = "18"
